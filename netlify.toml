# Publish must NOT be repo root — overrides Netlify UI/default (fixes plugin error)
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
